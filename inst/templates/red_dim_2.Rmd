
### Number of cells per grouing

<!-- Component created on {{ date }} -->

```{r, eval=!is_shiny}
sp_bars <- plotly::plot_ly(sd_{{ env_id }}, colors = "Set1", x = df_{{ env_id }}[[grouping]], color = df_{{ env_id }}[[grouping]]) %>%
  plotly::layout(
    xaxis = list(title = names(df_{{ env_id }}[grouping])),
    yaxis = list(title = "Number of observations"),
    barmode = "overlay",
    showlegend = FALSE
  ) %>%
  plotly::highlight("plotly_selected")
sp_bars
```

```{r, eval=is_shiny}
```

